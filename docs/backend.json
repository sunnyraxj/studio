
{
  "entities": {
    "Shop": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Shop",
      "type": "object",
      "description": "Represents a shop or business entity within the multi-tenant system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Shop entity."
        },
        "name": {
          "type": "string",
          "description": "Name of the shop."
        },
        "ownerId": {
          "type": "string",
          "description": "Reference to UserProfile. (Relationship: UserProfile 1:N Shop). The ID of the user who owns this shop."
        }
      },
      "required": [
        "id",
        "name",
        "ownerId"
      ]
    },
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Represents a user profile in the system.  Does not store authentication information.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserProfile entity."
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "Full name of the user."
        },
        "lastName": {
          "type": "string",
          "description": "Last name of the user."
        },
        "subscriptionStatus": {
          "type": "string",
          "description": "The user's current subscription status.",
          "enum": [
            "active",
            "inactive",
            "pending_verification",
            "rejected"
          ]
        },
        "shopId": {
          "type": "string",
          "description": "The ID of the shop this user belongs to."
        },
        "role": {
          "type": "string",
          "description": "The user's role in the system.",
          "enum": [
            "user",
            "admin"
          ]
        },
        "utr": {
          "type": "string",
          "description": "The Unique Transaction Reference (UTR) for the subscription payment."
        },
        "planPrice": {
          "type": "number",
          "description": "The price of the plan the user is subscribing to."
        },
        "rejectionReason": {
          "type": "string",
          "description": "The reason provided by an admin for rejecting a payment."
        }
      },
      "required": [
        "id",
        "email",
        "name",
        "subscriptionStatus",
        "role"
      ]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product in a shop's inventory.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
          "type": "string"
        },
        "price": {
          "type": "number"
        },
        "stock": {
          "type": "number"
        },
        "category": {
          "type": "string"
        },
        "sku": {
          "type": "string"
        },
        "status": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "name",
        "price",
        "stock",
        "status"
      ]
    },
    "Sale": {
      "type": "object",
      "title": "Sale",
      "description": "Represents a single sale transaction.",
      "properties": {
        "id": {
          "type": "string"
        },
        "date": {
          "type": "string",
          "format": "date-time"
        },
        "total": {
          "type": "number"
        },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "productId": {
                "type": "string"
              },
              "name": {
                "type": "string"
              },
              "quantity": {
                "type": "number"
              },
              "price": {
                "type": "number"
              }
            },
            "required": [
              "productId",
              "name",
              "quantity",
              "price"
            ]
          }
        }
      },
      "required": [
        "id",
        "date",
        "total",
        "items"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/UserProfile"
          },
          "description": "Stores user profile information. Path-based ownership ensures only the user can access their profile."
        }
      },
      {
        "path": "/shops/{shopId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/Shop"
          },
          "description": "Stores shop information. Includes denormalized 'ownerId' for authorization independence."
        }
      },
      {
        "path": "/shops/{shopId}/products/{productId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Products for a specific shop."
        }
      },
      {
        "path": "/shops/{shopId}/sales/{saleId}",
        "definition": {
          "schema": {
            "$ref": "#/backend/entities/Sale"
          },
          "description": "Sales records for a specific shop."
        }
      },
      {
        "path": "/shops/{shopId}/settings/{docId}",
        "definition": {
          "schema": {
            "type": "object"
          },
          "description": "Settings for a specific shop."
        }
      }
    ]
  }
}
